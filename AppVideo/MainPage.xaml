<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="AppVideo.MainPage"
             Title="AppVideo">

    <Grid Padding="24" RowDefinitions="Auto,Auto,*" RowSpacing="16">
        <Label Text="Videos em Destaque"
               Style="{StaticResource Headline}"
               SemanticProperties.HeadingLevel="Level1" />

        <Label Grid.Row="1"
               Text="Exemplo DDD: dados vindo do Use Case (Application)"
               Style="{StaticResource SubHeadline}" />

        <CollectionView Grid.Row="2"
                        ItemsSource="{Binding FeaturedVideos}"
                        SelectionMode="None">
            <CollectionView.EmptyView>
                <Label Text="Carregando videos..."
                       HorizontalOptions="Center"
                       VerticalOptions="Center" />
            </CollectionView.EmptyView>

            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="12"
                           Margin="0,0,0,8"
                           BorderColor="#E0E0E0"
                           CornerRadius="12">
                        <VerticalStackLayout Spacing="4">
                            <Label Text="{Binding Title}" FontAttributes="Bold" FontSize="16" />
                            <Label Text="{Binding Category}" FontSize="13" TextColor="#666666" />
                            <Label Text="{Binding DurationText}" FontSize="13" TextColor="#666666" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>

</ContentPage>